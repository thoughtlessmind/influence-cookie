import React from 'react'
import  {Box, Typography } from '@material-ui/core'


const CookiePreview1 = () =>{
  return(
    <Box>
      <Box style={{display:'flex', alignItem:'center'}}>
        <Typography>Can we store Cookie</Typography>
        <Typography>Inf</Typography>
      </Box>
    </Box>
  )
}

export default CookiePreview1